---
// This component wraps SpotifyEmbed in a React wrapper with client:only
// to ensure the CookieConsentProvider context is available.
import SpotifyEmbedComponent from "./SpotifyEmbed";

const { episode } = Astro.props;
---

<SpotifyEmbedComponent client:only="react" episode={episode} />
